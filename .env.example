# Server-only — used by /api/chat (Vercel serverless). NEVER prefix with VITE_,
# or it would be bundled into the browser and exposed. Set this in the Vercel
# project's Environment Variables, not committed here.
ANTHROPIC_API_KEY=your_claude_api_key_here
